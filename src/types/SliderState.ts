export type SliderState = {
    currentSlide: number,
    totalSlides: number,
    transform: number,
    transformOffset: number,
    visibleSlides: number,
    step: number,
    infinite: boolean,
    preventAnimation: boolean,
    disablePrev: boolean,
    disableNext: boolean,
};
